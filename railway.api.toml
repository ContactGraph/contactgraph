[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["packages/**", "migrations/**", "Dockerfile", "pyproject.toml", "uv.lock", "railway.api.toml"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
